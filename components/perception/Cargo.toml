[package]
name = "perception-encoder"
version = "0.1.0"
edition = "2021"
description = "no_std perception encoder: sensory frame -> embedding vector"
license = "MIT"

[lib]
name = "perception_encoder"
path = "src/lib.rs"

[profile.release]
opt-level = 3
lto = true
