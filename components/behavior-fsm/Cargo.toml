[package]
name = "behavior-fsm"
version = "0.1.0"
edition = "2021"
description = "no_std behavior FSM (Idle/Search/Approach/Act/Recover) — decision layer for seL4"
license = "MIT"

[lib]
name = "behavior_fsm"
path = "src/lib.rs"

[profile.release]
opt-level = 3
lto = true
