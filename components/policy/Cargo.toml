[package]
name = "policy-mlp"
version = "0.1.0"
edition = "2021"
description = "no_std MLP inference (weights trained off-device, run on-device)"
license = "MIT"

[lib]
name = "policy_mlp"
path = "src/lib.rs"

[profile.release]
opt-level = 3
lto = true
